# M0で必ず決めること（意思決定リスト）：

- 実行時24MB制約の「測定方法」を決める（tracemalloc または psutil）
- →ひとまず、tracemalloc,psutl両方を活用

- 配布方式：pyinstaller --onedir を使うか --onefile を使うか（--onefile は起動時に展開され一時的にメモリ/IOを食う）→ 推奨   --onedir
- →--ondirを使用する

- 画像フォーマット：パレット（8-bit PNG） を採用するか（推奨）
- →採用する

- BGM形式：ogg（ループ短め）にするか（推奨）
- →短めにする。可能なら30秒程度？

- マップのストレージ形式：JSONにするか、バイナリにするか → 開発はJSONで、リリース前にバイナリ化（学習のため最初は可読性優先）
- →開発はJSON、最終段階ではバイナリ化する

- ネットワーク交換の範囲：LAN限定 or インターネット対応 → M0では LAN限定 と決める（セキュリティ管理が容易）
- →M0ではLANとする